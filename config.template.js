// Copy this file to config.js and add your Supabase credentials:
// window.SUPABASE_URL = "https://your-project-id.supabase.co";
// window.SUPABASE_ANON_KEY = "your-anon-public-key";

window.SUPABASE_URL = "";
window.SUPABASE_ANON_KEY = "";

// Optional. The OAuth client ID from Google Cloud Console (a public value —
// it appears in the page). With it set, "Continue with Google" runs on this
// site's own origin and the consent screen names the app instead of
// <project>.supabase.co. Leave empty to use Supabase's redirect flow.
window.GOOGLE_CLIENT_ID = "";
